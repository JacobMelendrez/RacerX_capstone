let express,engine,bcrypt,sqlite3,open,cookieParser,grantAuthToken,lookUpUserFromAuthToken,res;_9af‍.x([["dbPromise",()=>dbPromise]]);_9af‍.w("express",[["default",["express"],function(v){express=v}]]);_9af‍.w("express-handlebars",[["engine",["engine"],function(v){engine=v}]]);_9af‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_9af‍.w("sqlite3",[["default",["sqlite3"],function(v){sqlite3=v}]]);_9af‍.w("sqlite",[["open",["open"],function(v){open=v}]]);_9af‍.w("cookie-parser",[["default",["cookieParser"],function(v){cookieParser=v}]]);_9af‍.w("./authorize",[["grantAuthToken",["grantAuthToken"],function(v){grantAuthToken=v}],["lookUpUserFromAuthToken",["lookUpUserFromAuthToken"],function(v){lookUpUserFromAuthToken=v}]]);_9af‍.w("express/lib/response",[["default",["res"],function(v){res=v}]]);









       const dbPromise = _9af‍.a("open",open)({
    filename: "data.db",
    driver: _9af‍.a("sqlite3",sqlite3).Database,
});_9af‍.j(["dbPromise"]);

const app = _9af‍.a("express",express)();

app.engine("handlebars", _9af‍.a("engine",engine)());
app.set('view engine', 'handlebars');

app.use(_9af‍.a("cookieParser",cookieParser)());
app.use(_9af‍.a("express",express).urlencoded({ extended: false}));
app.use('/static', _9af‍.a("express",express).static(__dirname + '/static'));

app.use(async (req, res, next) =>{
    const {authToken} = req.cookies;
    if(!authToken){
        //console.log('request from user not logged in');
        return next();
    }
    try {
        const user = await _9af‍.a("lookUpUserFromAuthToken",lookUpUserFromAuthToken)(authToken);
        //console.log('request from user', user.username);
        req.user = user;
    } catch (e) {
        next(e);
    }
    next();
})

app.get("/", async (req, res) => {
    //read messages from database
    //console.log('request user', req.user);
    const db = await dbPromise;
    const eventList = await db.all(
        `SELECT * FROM Events;`
    );
    /*await db.run("SELECT * FROM Events", async(err, res)=>{
        if (err) throw err;
        else{
            console.log('success')
            res.send(res)
        }
    });
    console.log('Query ran successfully');*/
    /*const messages = await db.all(`SELECT 
        Messages.id,
        Messages.content,
        Users.username as authorName
    FROM Messages LEFT JOIN Users WHERE Messages.authorId = Users.id`);
    console.log('messages', messages)*/
    _9af‍.g.console.log(eventList);
    res.render("home", { eventList, user: req.user });
});

app.get('/register', (req, res) =>{
    if(req.user){
        return res.redirect('/')
    }
    res.render('register')
})

app.get('/login', (req, res) =>{
    if(req.user){
        return res.redirect('/')
    }
    res.render('login')
})

//logs out the user when they are logged in
app.get('/logout', (req, res) =>{
    res.clearCookie('authToken');
    res.redirect('/');
})

app.get('/create_conference', (req, res)=>{
    res.render("create_conference", { user: req.user });
})

// Retrieves events and posts them on home page - Rishab
app.get("/event", async (req, res) =>{
    const db = await dbPromise;
    const events = await db.all(
        `SELECT
        Events.id,
        Events.title,
        Events.eventDescription,
        Events.startTime,
        Events.endTime
        Users.username as authorName
        FROM Events LEFT JOIN Users WHERE Events.authorId = Users.id`
    );
    res.render("home", {events, user: req.user});
});

app.post('/register', async (req, res)=>{
    const db = await dbPromise;
    const {
        username,
        email,
        password
    } = req.body;
    const passwordHash = await _9af‍.a("bcrypt",bcrypt).hash(password, 10);
    try {
        await db.run('INSERT INTO Users (username, email, password) VALUES (?,?,?);',
            username,
            email,
            passwordHash
        )
        const user = await db.get('SELECT id FROM Users WHERE email=?;', email);
        const token = await _9af‍.a("grantAuthToken",grantAuthToken)(user.id);
        res.cookie('authToken', token);
        res.redirect('/');
    } catch (e) {
        return res.render('register', { error: e})
    }
})

app.post('/login', async (req, res) =>{
    const db = await dbPromise;
    const {
        email,
        password
    } = req.body;
    try {
        const existingUser = await db.get("SELECT * FROM Users WHERE email=?", email);
        if(!existingUser){
            throw 'Incorrect login';
        }
        const passwordMatch = await _9af‍.a("bcrypt",bcrypt).compare(password, existingUser.password);
        if(!passwordMatch){
            throw 'Incorrect login'
        }
        const token = await _9af‍.a("grantAuthToken",grantAuthToken)(existingUser.id);
        res.cookie('authToken', token);
        res.redirect('/');
    } catch (e){
        return res.render('login', {error: e});
    }
})

/*app.post('/create_conference', async (req, res) =>{
    //write messages to database
    //messages.push(req.body.message);
    if(!req.user){
        res.status(401);
        return res.send("must be logged in to post messages");
    }
    const db = await dbPromise;
    await db.run('INSERT INTO Messages (content, authorId) VALUES (?, ?);', req.body.message, req.user.id);
    res.redirect('/');
});*/

// Added code for post conference details to DB - Rishab 
app.post('/create_conference', async(req, res) =>{
    //Write event details to db
    const db = await dbPromise;
    const {
        event_title,
        event_description,
        zoom_link
    } = req.body;

    _9af‍.g.console.log(req.body);
    _9af‍.g.console.log(req.user.id);
    try{
        await db.run('INSERT INTO Events (authorId, title, eventDescription, zoomLink) VALUES (?, ?, ?, ?);', req.user.id, event_title, event_description, zoom_link);
        console.log('Data inserted successfully');
    }
    catch (e){
        _9af‍.g.console.log('Data was not inserted successfully', e);
        return res.render('create_conference', {error: e})

    }
    res.redirect('/')
});

function cancel(){
    _9af‍.a("res",res).render('create_conference');
}

location.reload();

const setup = async () => {
    const db = await dbPromise;
    await db.migrate();

    app.listen(8080, () =>{
        console.log("listening on http://localhost:8080"); //this line does not matter. just informative.
    });
}

setup();
