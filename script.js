/*function displayEventChoiceList() {
    document.getElementById("createEvent_list").classList.toggle("show");
  }
  
  // Close the dropdown if the user clicks outside of it
  window.onclick = function(event) {
    if (!event.target.matches('.event_dropdown')) {
      var dropdowns = document.getElementsByClassName("create_event_dropdown_list");
      var i;
      for (i = 0; i < dropdowns.length; i++) {
        var openDropdown = dropdowns[i];
        if (openDropdown.classList.contains('show')) {
          openDropdown.classList.remove('show');
        }
      }
    }
  }*/